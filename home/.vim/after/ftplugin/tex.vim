call PlainText()

" Softwrap long lines for collaborating with others
setlocal wrap linebreak

" Convert line endings to unix, if necessary
" setlocal ff=unix|update

" Turn off syntax errors, which don't work with custom macros
let g:tex_no_error=1
